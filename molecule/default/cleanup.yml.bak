---
- name: Cleanup
  hosts: all
  gather_facts: false
  tasks:
    - name: Output cleanup status message
      ansible.builtin.debug:
        msg: "Cleanup tasks are being executed, and the environment is being finalized."
